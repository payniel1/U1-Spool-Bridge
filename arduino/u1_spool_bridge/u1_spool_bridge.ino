// u1-spool-bridge — Arduino IDE entry point.
//
// Deliberately empty. setup() and loop() live in main.cpp alongside the rest
// of the firmware; the Arduino IDE just needs a .ino file with the same name
// as this folder in order to open the sketch.
//
// Before you compile, read ARDUINO-README.txt in this folder — there are two
// menu settings you have to get right, and one file to edit.
